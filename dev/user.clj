(ns user
  (:require [clojure.spec.test.alpha :as stest]
            [kafka-clj-utils.producers]
            [kafka-clj-utils.schema-registry]
            [kafka-clj-utils.utils]))

(stest/instrument)
